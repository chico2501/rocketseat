import React from 'react';

import { Container, Button } from './styles';

export default function Footer() {
  return (
    <Container>
      <Button onPress={() => {}}>Check-ins</Button>
      <Button onPress={() => {}}>Pedir ajuda</Button>
    </Container>
  );
}
